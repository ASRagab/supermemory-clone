{
  "mcpServers": {
    "supermemory": {
      "command": "node",
      "args": ["dist/mcp/index.js"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      },
      "cwd": "${PROJECT_ROOT}"
    }
  }
}
